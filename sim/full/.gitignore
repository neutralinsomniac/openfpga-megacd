# Verilator build output
obj_dir/

# rendered frame dumps
frames/
frames_cue/

# BIOS-derived golden data (copyrighted — regenerate locally, never commit)
*.hex

# end-of-run debug dumps (PRG/CDC RAM, etc.)
*.bin

# copied from rtl/FX68K by build.sh
*.mem

# generated by build.sh's REALSD path (sed transform of rtl/megacd/sdram.sv)
sdram_ctrl_gen.v

# freeze-repro PC ring dumps (tb_full end-of-run)
*_ring.txt

# Verilator --savable build (build_savable.sh) + checkpoint scratch dir
obj_savable/
ckptrun/
